#!/usr/bin/env bash

set -e

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo " Switching Rainier to unstable..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo

# Remove the existing channel if it exists
sudo nix-channel --remove nixos 2>/dev/null || true

# Add the unstable channel (fixed line break and trailing spaces)
sudo nix-channel --add https://channels.nixos.org/nixos-unstable nixos

# Update channels to apply changes
sudo nix-channel --update

echo
echo "✓ Unstable channel enabled."
echo

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo " Rebuilding Rainier system..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo

# Rebuild the system configuration for the next boot
sudo nixos-rebuild boot

echo
echo "✓ System rebuilt."
echo

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo " Final Step"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo
echo "After reboot run:"
echo
echo "bootstrap"
echo
echo "Bootstrap will install:"
echo "  • Dotfiles"
echo "  • Scripts"
echo "  • Themes"
echo "  • User configuration"
echo
echo "Rebooting in 10 seconds..."
echo

sleep 10

sudo reboot
